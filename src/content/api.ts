import { Course } from './types';

export async function fetchCourses(): Promise<Course[]> {
  // Placeholder for DB fetch
  return [];
}

export async function createCourse(data: any): Promise<Course> {
  // Placeholder for DB create
  return {} as Course;
}
